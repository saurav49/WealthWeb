export enum EmailInviteStatus {
  NOT_SENT = 'not_sent',
  FAILED = 'failed',
  INVITED = 'invited',
  ACCEPTED = 'accepted',
}

export enum Role {
  'Company' = 'company',
  'Investor' = 'investor',
}
